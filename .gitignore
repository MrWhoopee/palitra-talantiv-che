node_modules/
.pnpm-store/

.next/
out/
dist/
build/
*.tsbuildinfo

.turbo/

.env
.env.local
.env.*.local

apps/api/src/generated/

coverage/
.vitest/

.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Personal Claude Code overrides for this checkout - not a team policy.
.claude/settings.local.json

# Завантажені зображення: локальне сховище, не вміст репозиторію
apps/api/var/

# Downloaded source models and their archives: heavy, and nothing imports
# them yet. Anything the show actually ships gets added back deliberately,
# converted and compressed, not as a raw FBX.
/apps/web/public/demo/models/
